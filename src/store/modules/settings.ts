export interface SettingsState {
  auth: true
}

const state = {
  auth: []
}
const mutations = {}
const actions = {}
const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
